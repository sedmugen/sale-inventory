# ==============================================================================
# SALE & INVENTORY MANAGEMENT SYSTEM - ENVIRONMENT CONFIGURATION TEMPLATE
# Copy this file to .env or configure in your deployment environment
# ==============================================================================

# --- Server (Spring Boot) Configuration ---
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=dev

# --- Database Configuration ---
DB_HOST=localhost
DB_PORT=3306
DB_NAME=sales-inventory-db
DB_USERNAME=root
DB_PASSWORD=your_secure_password

# --- Client (React) Configuration ---
REACT_APP_API_BASE_URL=http://localhost:8080/api
PORT=3000
